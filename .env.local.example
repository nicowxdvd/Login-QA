# Base URL of your local NestJS auth API (server-side only, never exposed to the browser).
# Adjust the port to match where your NestJS instance is actually listening.
AUTH_API_URL=http://localhost:3001
